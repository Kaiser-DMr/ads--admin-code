{
  "name": "ad-admin-backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "PORT=${PORT:-3002} node --experimental-sqlite server.js",
    "dev": "PORT=${PORT:-3002} node --experimental-sqlite --watch server.js",
    "test": "AD_ADMIN_DB_PATH=:memory: AD_ADMIN_SEED=false node --experimental-sqlite --test tests/*.test.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.3",
    "supertest": "^7.0.0"
  }
}
